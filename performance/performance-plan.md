## Часть 4: Производительность в распределённых системах

*(рабочий план сценария)*

---

### 🔥 C. Resource Optimization Trade-offs

*(когда оптимизация одной метрики ломает три других)*

#### 1. Боль

Ты уменьшил latency → выросла стоимость.
Ты увеличил throughput → упало качество данных.
Ты уменьшил стоимость → упал SLA.
Ты увеличил кэш — уменьшил согласованность.
Ты включил aggressive retries → убил backend.

Всё стоит чего-то. Нет бесплатных оптимизаций.

---

#### 2. Что разобрать (структурно)

1. **Latency vs Consistency**

    * кэш ускоряет → теряем strong-consistency
    * eventual consistency в обмен на скорость

2. **Throughput vs Accuracy**

    * sampling, probabilistic caching, bloom filters
    * approximate counters (HyperLogLog)

3. **Latency vs Cost**

    * больше инстансов → быстрее, но дорого
    * CDN дешёвый, но имеет stale window

4. **Consistency vs Availability**

    * CAP trade-off
    * если хочешь не падать — теряешь жёсткую консистентность

5. **Caching vs Correctness**

    * stale data
    * cache invalidation как цена скорости

6. **Parallelism vs Contention**

    * больше потоков → больше блокировок

---

#### 3. Примеры боли

* агрессивные retries устраивают retry-storm
* оптимизация базы с кэшированием ломает согласованность
* уменьшили количество воркеров → увеличилась latency через очередь
* увеличили количество воркеров → выросла конкуренция → система стала медленнее
