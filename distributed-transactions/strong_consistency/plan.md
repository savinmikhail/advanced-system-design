# 🔥 **Strong Consistency: определение, боль, план**

---

# 🧨 1. Определение (простым, человеческим языком)

**Strong consistency** — это гарантия, что все клиенты видят данные **как будто существует один единственный источник истины**, и каждое чтение после записи получает **последнее подтверждённое состояние**.

Формально:

> После успешной записи любой последующий read увидит эту запись.
> Независимо от того, куда этот read попал — на реплику, на другой шардинг, на другой инстанс.

Это поведение монолита с одной базой.
Это то, как *люди ожидают, что IT работает*.
Но так почти не работает ничего, что распределено.

---

# 💥 2. Почему Strong Consistency — дорогая роскошь

Для strong consistency почти всегда нужно:

* синхронно обновлять все реплики
* держать распределённые блокировки
* ждать подтверждений от всех нод
* иметь один глобальный сериализатор (sequencer)
* жертвовать доступностью и временем ответа

А сеть:

* теряет пакеты
* лагает
* рвёт соединения
* иногда умирает

Поэтому на практике strong consistency:

* **дорогая**
* **медленная**
* **плохо масштабируется**
* **часто разрушает SLA**

---

# 🎯 3. Что рассказать в этой части (структура)

Ниже — план, по которому можно делать обучающий блок или видео.

---

## **3.1. Что значит “сильная консистентность”**

Объяснить строго и практично:

### ✔ Linearizability (линейризуемость)

Самое строгое определение консистентности.

> Все операции происходят *атомарно* и *в одном общем порядке*,
> даже если выполняются на разных машинах.

Это то, как работает один процессор.
Это то, чего ожидают программисты.
Это то, что почти невозможно в распределённых системах.

### ✔ Sequential consistency

Чуть слабее: порядок внутри каждого клиента сохраняется, глобальный порядок — не обязательно.

### ✔ Read-after-write (RAW)

Минимальная форма strong consistency:
Клиент читает свои же записи.

---

## **3.2. Где требуется strong consistency**

* банковские переводы
* списание денег
* инвентаризация на складе
* биллинг
* ACL (доступы)
* security
* транзакции, которые невозможно компенсировать
* операции, где ошибка стоит дорого

Это надо подчеркнуть:
strong consistency ≠ “красиво”.
Её вводят **только там, где ошибка стоит дорого**.

---

## **3.3. Чем достигают strong consistency (механизмы)**

Вот здесь начинается практическая часть.

### ✔ 1. Single leader

Один узел пишет, все остальные читают.

👉 Самый распространённый вариант.
Postgres, MySQL, Redis primary/replica.

Минусы:

* лидер падает → пауза
* write throughput ограничен одним узлом

### ✔ 2. Синхронная репликация

Пишем на несколько нод синхронно.

👉 Strong consistency достигается ценой latency.

Проблемы:

* ожидание ack всех узлов
* если один медленный — система медленная

### ✔ 3. Distributed lock managers

Например:

* etcd locks
* Zookeeper
* Chubby

Используются как глобальные “источники истины”
для сериализации операций.

Проблемы:

* требуют специальных хранилищ
* стоят дорого (latency jumps)
* все write проходят через один bottleneck

### ✔ 4. TrueTime (Google Spanner)

Фантастика, но реальная:

* глобальные атомные часы
* bound clock skew
* можно делать распределённые транзакции с strong consistency

Но:

* дорого
* доступно только в их облаке
* подходит не всем задачам

---

## **3.4. Цена strong consistency (trade-offs)**

Вот тут нужно объяснять честно.

### 💸 1. Latency ↑

Синхронность = медленно.

### 🧱 2. Availability ↓

По CAP:

> если хотим Strong Consistency — должны жертвовать Availability.

Любой узел может “зависнуть”, и тогда система зависает.

### ⚠ 3. Throughput ↓

С ростом числа реплик write throughput падает.

### 🧨 4. Tail latency взлетает

Самая медленная реплика определяет весь кластер.

### 🔄 5. Риск global stalls

Система зависает на минуту из-за проблем лидера.

---

## **3.5. Инженерная практика: когда сильная консистентность не нужна**

Надо честно сказать:

> 90% запросов в бизнесе НЕ требуют strong consistency.

Для большинства задач eventual consistency подходит идеально:

* корзины
* каталоги
* лайки
* ленты
* профили
* кеш
* аналитика

Strong consistency нужна редко — но жизненно важно там, где она нужна.

---

## **3.6. Как ужиться со strong consistency, если деваться некуда**

Практика:

### ✔ Репликация с majority quorum

Типичная обвязка around RAFT/Paxos.

### ✔ Улучшение latency сети

Потому что latency определяет время подтверждения.

### ✔ Pre-sharding write path

Чтобы не было глобального сериализатора.

### ✔ Комбинация RAW + eventual read для остальных

Часто даёт идеальный баланс.

### ✔ Локальные кэши с write-through

### ✔ Request pinning

### ✔ Idempotency everywhere

Даже в strong-consistent системах могут быть повторы.

---


# 🧩 Итог (коротко)

Strong consistency — это:

✔ предсказуемые чтения
✔ отсутствие гонок
✔ отсутствие рассогласованности
✔ безопасность транзакций

Но цена:

⚠ медленнее
⚠ дороже
⚠ хуже доступность
⚠ хуже масштабируемость

Поэтому её применяют *точечно*, а не везде.
