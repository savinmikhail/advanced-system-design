# Часть 1. Продвинутое кэширование и консистентность кэша
## План всей части (A–C) и опорные блоки

Этот файл — рабочие заметки для подготовки сценария. README.md ассистент не трогает, сюда можно копировать/адаптировать тезисы.

Важно: здесь сразу учитываем фидбек модерации по предыдущим главам.

- Никакой «воды» и голых перечислений стратегий: каждый пункт должен иметь «что это / зачем / где в работе / где на собесе / как на схеме».
- Всегда есть реальный кейс (желательно один домен на протяжении части — интернет‑магазин/маркетплейс/доставка).
- Для любых списков вопросов — есть примеры ожидаемых ответов и пояснение, как они влияют на архитектуру.
- Меньше терминов, больше глубины: выбираем 2–3 ключевых паттерна, остальные упоминаем вскользь или выносим в «для любопытных».

---

## Что уже сказано про кэш в главах 1–2

До нашей части курс уже упоминает кэш, но без глубокой проработки:

- В главе 1:
    - на уровне архитектурной схемы: «микросервис общается с базой данных и/или кэшем»;
    - один пример: «часто запрашиваемые данные (история заказов) могут храниться в кэше, это поможет сократить задержки».
- В главе 2:
    - блок «Данные и кэш»: PostgreSQL как основная база, реплики для чтения, Redis как кэш для «горячих» данных (ближайшие выгульщики, сессии), TTL, S3 для файлов;
    - блок «Чтение и запись»: «для чтений подключаем реплику базы и кэш Redis»;
    - блок «Масштабирование кэша и очередей»: Redis как кэш, кластер Redis для горизонтального масштабирования.

То есть:
- уже показано, что кэш существует и что он «ускоряет» и снимает нагрузку;
- упомянут Redis, TTL и масштабирование кэша;
- но почти нет:
    - формального определения и модели «что такое кэш и где он живёт в системе»;
    - терминов и паттернов (cache-aside, write-through, stampede и т.п.);
    - подробного разбора проблем (устаревшие данные, инвалидация, гонки, dogpile);
    - разговоров про многослойное кэширование, тегирование и версионирование.

Вывод: в нашей части не нужно объяснять кэш «с нуля для джунов», но стоит дать короткий, практический advanced‑рефреш, который логично продолжит главу 2 и подведёт к cache coherence.

---

## Общий каркас части 1 и домен примеров

- Домен: интернет‑магазин / маркетплейс (уровня Ozon/Wildberries, но без брендов), где есть:
    - каталог товаров с фильтрами и сортировкой;
    - карточка товара с ценой, остатками на складе, рейтингом и отзывами;
    - корзина и оформление заказа;
    - личный кабинет с историей заказов;
    - промо‑механики (скидки, персональные рекомендации).
- Структура части:
    - Блок 1 — модель кэша и базовые паттерны (общий ввод, единая терминология для A–C);
    - Блок 2 (A) — cache coherence в распределённых системах;
    - Блок 3 (B) — многоуровневое кэширование и оптимизации;
    - Блок 4 (C) — сложные проблемы кэширования и практические кейсы.
- Нарратив части:
    - короткий рекап и «боль» от кэша → модель кэша → cache coherence как главный концептуальный блок → многоуровневые оптимизации → «тёмная сторона» и собес.

---

## Визуализации и схемы для части 1

Для удобства монтажа и подготовки материалов — список всех схем:

### Блок 1:
- **[СХЕМА 1]** Пирамида кэшей (клиент → браузер → CDN → backend → Redis → БД)
- **[СХЕМА 2]** Flow Cache-Aside для запроса товара (код + timeline)

### Блок 2 (A):
- **[СХЕМА 3]** Timeline race condition в Cache-Aside
- **[СХЕМА 4]** Multi-level cache с рассинхроном (стрелки показывают расхождение)
- **[ТАБЛИЦА 1]** Сравнение стратегий coherence (TTL vs Event-based vs Lease)

### Блок 3 (B):
- **[СХЕМА 5]** Read path через L1 → L2 → БД с метриками latency
- **[HEATMAP]** Матрица "тип данных × слой кэша" (что где держим)

### Блок 4 (C):
- **[ГРАФИК 1]** Thundering herd timeline (QPS на кэш и БД)
- **[TIMELINE 2]** Реальный инцидент по минутам (симптомы → root cause → fix)
- **[СХЕМА 6]** Multi-tenant bug (забыли tenant_id в ключе)

Все схемы удобно делать в Excalidraw или Miro, оставляя место для анимаций.

---

## Блок 1. Модель кэша и базовые паттерны (общий ввод ко всей части)

Этот блок идёт в начале части 1 и «склеивает» подпункты A, B, C. Время: 5–7 минут.
На уровне курса кэш уже появлялся в главах 1–2 как «коробочка Redis рядом с базой», которая разгружает БД и ускоряет ответы. Здесь мы переходим от картинок к «недрам» — к тому, где кэш реально спасает систему и где позволяет легко прострелить себе ногу.

### 1. Кэш как часть архитектуры, а не просто «ускоритель»
- Зачем вообще всё это городить:
    - разгрузка основной БД и внешних API;
    - ускорение горячих чтений (карточка пользователя, список ближайших объектов, состояние заказа);
    - переиспользование дорогих вычислений (агрегации, рекомендации, отчёты);
    - отдельные кейсы вроде rate limiting и feature‑флагов.
- Пример боли со сторонним сервисом:
    - для оформления покупки нужно дернуть внешний сервис курсов валют;
    - курсы нужны быстро и часто, но внешний API не наш и легко словит DDoS с наших 200k пользователей;
    - решение: отдельный воркер периодически вычитывает курсы и кладёт их в кэш, а фронт/бекенд читают уже из кэша, а не лупят внешний API напрямую.
- Что такое кэш в терминах архитектуры: «быстрый, ограниченный, потенциально устаревающий слой рядом с медленным, но источником истины».
- Где он обычно стоит: перед базой, перед сторонним API, перед тяжёлой бизнес‑логикой.
- Какие метрики и ограничения важны: latency, hit rate, пропускная способность, объём, TTL.
- Базовые термины, на которые дальше будем опираться:
    - `cache hit` / `cache miss`;
    - hot key vs холодный кэш;
    - TTL, эвикция, stale data.

### 2. Слои кэширования в реальных системах и базовый паттерн
- Краткий список слоёв (по 1–2 предложения):
    - Браузер/HTTP — статика, API-ответы с Cache-Control/ETag
    - CDN/edge — медиа, популярные страницы
    - In-process (L1) — справочники, конфиги, правила
    - Distributed cache (L2, Redis) — сессии, горячие данные, агрегаты
    - БД-уровень — индексы, материализованные представления

- **[СХЕМА 1]** Пирамида кэшей в маркетплейсе:
  ```
  Клиент → Браузер → CDN → API Gateway → Backend (L1) → Redis (L2) → БД
  ```
  Что где лежит:
    - Браузер: JS/CSS/картинки, куски каталога
    - CDN: медиа, страницы товаров, статика
    - L1 (in-process): справочники категорий, права, фича-флаги
    - L2 (Redis): сессии, корзины, популярные товары, счётчики
    - БД: source of truth, индексы, материализованные вьюхи для отчётов

- **Базовый паттерн Cache-Aside** (разбираем подробно, остальные паттерны упоминаем):

  **[СХЕМА 2]** Flow запроса карточки товара:
  ```php
  public function getProduct(int $productId): array
  {
      $cacheKey = sprintf('product:%d', $productId);

      // 1. Проверяем Redis / Symfony Cache
      $cached = $this->cache->get($cacheKey, function (ItemInterface $item) use ($productId) {
          $item->expiresAfter(300); // 5 минут TTL

          // 2. Идём в БД
          return $this->connection->fetchAssociative(
              'SELECT * FROM products WHERE id = :id',
              ['id' => $productId]
          );
      });

      return $cached;
  }
  ```

  Запрос "товар ID=123":
    - L1 (in-process) → miss
    - L2 (Redis) → miss
    - БД → HIT (latency: 50ms)
    - Кладём в L2 и L1
    - Следующий запрос: L2 → HIT (latency: 2ms) ✅

- Проблема: а что происходит при обновлении?
  → Подводка к race condition и coherence в Блоке 2

- Детали HTTP-заголовков, Service Worker, эвикции, других паттернов (read-through, write-through) → выносим в Блоки 2–3 как материал для углублений.

### 3. Мост к последующим подпунктам
- Подводка: теперь у нас не один кэш, а пирамидка слоёв, в каждом из которых лежат копии одних и тех же данных.
- Три главных вызова, с которыми дальше будем жить всю часть:
    - как сделать так, чтобы эти копии не противоречили друг другу (подводка к подпункту A — cache coherence);
    - как именно распределить данные по уровням кэша и что где держать (подводка к подпункту B — многоуровневое кэширование);
    - что делать, когда всё это ломается под нагрузкой (подводка к подпункту C — сложные проблемы кэширования).
- В сценарии это можно проговорить как явный мост: «Дальше у нас три подпункта, и каждый отвечает на один из этих вопросов».

---

## Блок 2 (A). Cache Coherence в распределённых системах

Ориентир: 10–15 минут, можно ужимать/расширять.

1. **Зачем вообще говорить про cache coherence в распределённых системах**
    - Приводим короткий инцидент из маркетплейса: пользователь меняет адрес доставки или способ оплаты, а часть интерфейса продолжает показывать старые данные.
    - Под капотом: профиль хранится в БД, локальном кэше сервиса, общем Redis и частично в CDN/браузере — слои обновляются в разное время.
    - У нас не один кэш, а много: локальные кэши сервисов, общий Redis/Memcached, CDN, браузер и т.д.
    - Каждая новая копия ускоряет чтения, но увеличивает риск противоречий.
    - Интуитивная проблема: «почему пользователю показывают старые данные, хотя запись в БД уже обновилась?».

1.5. **Race condition в Cache-Aside: почему coherence — это проблема**

Обновление товара и классическая гонка (пример на PHP/Symfony-подобном стиле):
   ```php
   public function updateProduct(int $productId, float $newPrice): void
   {
       // 1. Обновляем БД (через Doctrine DBAL / ORM)
       $this->connection->executeStatement(
           'UPDATE products SET price = :price WHERE id = :id',
           ['price' => $newPrice, 'id' => $productId]
       );

       // 2. Инвалидируем кэш (Symfony Cache, Redis-клиент и т.п.)
       $cacheKey = sprintf('product:%d', $productId);
       $this->cache->delete($cacheKey);
   }
   ```

**[СХЕМА 3]** Timeline race condition:
   ```
   t=0: Thread A начинает updateProduct(123, price=1000)
   t=1: Thread A обновляет БД: products[123].price = 1000 ✅
   
   t=2: Thread B вызывает getProduct(123)
   t=2: Thread B проверяет Redis → miss (ключ ещё не удалён)
   t=2: Thread B читает из БД → получает price=1000 (новое значение)
   
   t=3: Thread A выполняет redis.delete("product:123")
   
   t=4: Thread B записывает в Redis: product:123 = {price: 900} ← СТАРОЕ! ❌
   
   Результат: кэш содержит устаревшее значение price=900, 
   хотя БД уже price=1000
   ```

Вопрос аудитории: "Как часто это реально бывает?"
- При 10k RPS и 100ms окне гонки → ~1000 запросов попадают в проблемное окно
- Из них ~1% могут записать stale данные → 10 багов в секунду
- Для критичных данных (деньги, лимиты) это неприемлемо

Решения разберём дальше в пунктах 5–7.

2. **Определения: cache coherence vs consistency**
    - Cache coherence — про согласованность _копий одного и того же объекта_ между разными кэшами.
    - Отстроиться от «консистентности данных в БД» и от CAP-теоремы.
    - Условие: «нет двух кэшей с противоречащими значениями» + обсудить, насколько быстро мы этого требуем.
    - Можно упомянуть подход Polaris из Meta: отдельный сервис, который асинхронно ходит по кластерам кэша, выборочно сравнивает значения с source of truth и считает метрику нарушений coherence на разных таймфреймах — это хороший пример того, что в реальности мы часто не гарантируем идеальную coherence, а системно измеряем и контролируем уровень расхождений.

3. **Мысленная модель: аналогия с процессорными кэшами, но в более «грязном» мире**
    - В CPU есть протоколы MESI и жёсткий порядок операций.
    - В распределённой системе:
        - ненадёжная сеть,
        - непредсказуемые задержки,
        - нет глобальных часов.
    - Хочется ощущения «как будто данные в одном месте», но платим ростом сложности и latency.

4. **Типичные архитектуры кэшей и где там всплывает coherence**
    - Один общий распределённый кэш-кластер (Redis/Memcached) поверх БД: coherence проще, но есть TTL, репликация, шардирование.
    - Многоуровневый кэш:
        - L1 — локальный in-memory кэш в каждом сервисе;
        - L2 — общий Redis;
        - L3 — БД.
    - CDN / edge-кэши поверх API.
    - На каждом уровне — свой компромисс между свежестью, latency и сложностью.

5. **Базовые паттерны взаимодействия с кэшем и их влияние на coherence**

   Быстро освежаем паттерны (детально разбирали cache-aside в Блоке 1):

    - **Cache-aside** (lazy loading) — самый частый:
        - Сервис сам идёт в БД при miss
        - ✅ Простота, гибкость
        - ❌ Проблема race condition (разобрали выше)

    - **Read-through / write-through**:
        - Кэш-слой сам ходит в БД, каждое чтение/запись проходит через него
        - ✅ Централизация логики
        - ❌ Кэш становится критичным компонентом (SPOF)

    - **Write-behind** (write-back):
        - Запись сначала в кэш, затем асинхронный сброс в БД
        - ✅ Низкая latency записи
        - ❌ Риск потери данных при падении кэша

   Для каждого паттерна — свои проблемы с coherence:
    - Cache-aside: race condition при инвалидации (уже обсудили)
    - Read/write-through: проблема при репликации кэша между ДЦ
    - Write-behind: что если два узла кэша не знают о записях друг друга?

6. **Механизмы обеспечения согласованности (2–3 стратегии)**

   Разбираем не весь зоопарк, а характерные подходы:

   **А. TTL-only / "ленивая" согласованность:**
    - Просто ждём, пока данные протухнут
    - ✅ Дешёво, просто
    - ❌ Позволяет жить со stale данными до истечения TTL
    - Когда подходит: ленты, счётчики лайков, некритичная статистика
    - Пример: каталог товаров с TTL=60 секунд

   **Б. Явная инвалидация по событиям:**
    - Pub/sub, message bus, CDC (Change Data Capture)
    - ✅ Предсказуемое окно рассинхрона (обычно <1 секунды)
    - ❌ Дороже в реализации, нужна инфраструктура
    - Когда подходит: профили пользователей, корзины
    - Пример (PHP + Symfony Messenger / EventBus):
      ```php
      // Сервисный метод, который обновляет товар и публикует событие
      public function updateProduct(int $productId, array $data): void
      {
          $this->productRepository->update($productId, $data);

          // Публикуем доменное событие в шину (Messenger, Kafka, RabbitMQ и т.п.)
          $this->eventBus->dispatch(new ProductUpdatedEvent($productId));
      }

      // Обработчик события (слушатель Symfony Messenger)
      final class InvalidateProductCacheHandler
      {
          public function __construct(
              private CacheInterface $cache,
          ) {}

          public function __invoke(ProductUpdatedEvent $event): void
          {
              $cacheKey = sprintf('product:%d', $event->getProductId());

              // Инвалидируем кэш Redis / Symfony Cache
              $this->cache->delete($cacheKey);

              // если есть локальный L1-кэш в сервисе — чистим и его
          }
      }
      ```

   **В. Lease / token-based подходы:**
    - Только владелец "лизинга" имеет право обновлять кэш
    - При обновлении в БД: все лизинги автоматически аннулируются
    - ✅ Гарантия от race condition
    - ❌ Сложность реализации, overhead
    - Когда подходит: критичные данные (балансы, лимиты)
    - Связь с fencing tokens и distributed locks

   **[ТАБЛИЦА 1]** Сравнение стратегий:
   ```
   | Стратегия       | Окно рассинхрона | Сложность | Примеры use-cases          |
   |-----------------|------------------|-----------|----------------------------|
   | TTL-only        | До TTL (секунды) | Низкая    | Ленты, счётчики           |
   | Event-based     | <1 секунды       | Средняя   | Профили, корзины          |
   | Lease-based     | Нет              | Высокая   | Балансы, лимиты, инвентарь|
   ```

   Инвалидация vs обновление:
    - Инвалидация (delete) проще и безопаснее — следующий запрос сам подгрузит свежие данные
    - Обновление (set new value) быстрее, но рискует записать stale данные при гонках
    - Golden rule: "При сомнении — инвалидируй"

   Push vs pull:
    - Push: кэш получает события извне (pub/sub) → быстрее
    - Pull: кэш сам запрашивает обновления → проще, но дольше

7. **Сложные кейсы и баги**
    - Одновременные запросы: гонки при записи, lost update, грязное чтение.
    - Split-brain и расхождение между узлами кэша.
        - отметить, что в блоке 4 будет живой инцидент со split-brain.
    - Примеры:
        - профиль пользователя;
        - баланс/лимиты;
        - корзина/остатки товара;
        - rate limiting.

8. **Trade-off'ы: цена сильной когерентности**
    - Сильная когерентность = больше синхронизации между узлами, выше latency и стоимость.
    - Где достаточно eventual coherence (лента, счётчики лайков).
    - Где нужна «почти» strong coherence (деньги, лимиты, инвентарь) и какие паттерны помогают:
        - версионирование (ETag, версии записей);
        - CAS/compare-and-set;
        - fencing tokens, лизинговые блокировки.

9. **Наблюдаемость и тестирование когерентности**
    - Метрики:
        - доля stale reads (через версионирование или временные метки);
        - hit/miss каждого уровня кэша;
        - частота инвалидаций (events/second);
        - latency инвалидации (время от записи в БД до удаления из всех кэшей).

    - Chaos/latency injection:
        - как проверить, что при задержках и падениях модель согласованности не разваливается;
        - пример теста: вносим 500ms delay в pub/sub и смотрим, растёт ли доля stale reads.

    - Типичные вопросы на собеседовании:
        - "Какие гарантии по свежести данных в кэше вы даёте?"
          Хороший ответ: "Для каталога — eventual consistency с окном до 60 секунд (TTL).
          Для балансов — read-through с БД, кэш только для справки."

        - "Как убедиться, что кэш и БД не расходятся?"
          Хороший ответ: "Версионирование ключей, метрики stale reads,
          периодический reconciliation job."

   **Визуализации для Блока 2:**
    - **[СХЕМА 3]** Timeline race condition (уже есть в п. 1.5)
    - **[СХЕМА 4]** Multi-level cache с рассинхроном:
      ```
      Браузер [price=900] ─╮
                            ├─ рассинхрон! 
      CDN [price=900] ──────┤
                            │
      Redis [price=1000] ───┤
                            │
      БД [price=1000] ──────╯ ← source of truth
      ```
    - **[ТАБЛИЦА 1]** Сравнение стратегий coherence (уже есть в п. 6)

---

## Блок 3 (B). Многоуровневое кэширование и оптимизации

Ориентир: 10–15 минут.

Перед тем как уходить в схемы, проговариваем боль: мы хотим отдавать данные как можно ближе к пользователю — зачем тянуть картинку из S3, если её может отдать CDN, или каждый раз ходить в Redis, если сервис может держать маленький L1‑кэш у себя в памяти. Чем ближе слой к запросу, тем ниже latency и нагрузка на нижестоящие уровни, но с каждым новым уровнем нужно честно отвечать себе, какие данные где держать и какой реальный выигрыш мы получаем, а где просто плодим сложность.

1. **Многоуровневая схема кэширования в нашем домене**
    - Конкретная картинка: клиент → CDN/edge → API‑шлюз → backend‑сервисы (с L1‑кэшами) → Redis‑кластер → БД/хранилища.
    - Что на каком уровне держим: статика, результаты API, агрегаты, сессии, справочники и т.п.
    - Берём ту же схему, что и в блоке 1, но теперь смотрим на неё через призму latency и стоимости.
    - Визуально: одна большая схема + простая heatmap «тип данных × слой кэша», которая помогает обсуждать, что куда выгодно складывать.

2. **Чтения через несколько уровней кэша**
    - Типичный read‑flow: клиентский кэш → edge → L1 → L2 → БД.
    - Где хотим максимальный hit‑rate и почему (обычно L1+L2).
    - Как multi-level кэш влияет на tail latency: где выгодно положить ещё один слой, а где нет.

3. **Записи и обновления в многоуровневой системе**
    - Порядок обновления слоёв: сначала источник истины, потом инвалидация/обновление кэшей.
    - Выбор стратегии: «invalidate‑all» vs «invalidate‑by‑key/tag» vs «lazy refresh on next read».
    - Тегирование и группировка ключей:
        - зачем группировать связанные ключи (все данные пользователя, все данные товара);
        - как тегированная инвалидация помогает при массовых изменениях (например, при деактивации продавца).
    - Версионирование кэша:
        - включение версии схемы/данных в ключ или отдельное поле;
        - связь с blue‑green деплоем и dual‑write: старая и новая модель живут параллельно на разных наборах ключей.
    - Trade‑off'ы: чем больше уровней и «умнее» инвалидация, тем сложнее поддерживать согласованность и отлаживать баги.

4. **Оптимизации под реальные паттерны трафика**
    - Горячие ключи (hot keys) и их обработка:
        - распределение нагрузки по нескольким шардам/репликам кэша;
        - запросы через отдельный слой/сервер для горячих данных.
    - Cache-ahead / pre-warming:
        - прогрев популярных маршрутов/точек/подборок перед пиком;
        - как не «убить» БД прогревом.
    - Request coalescing / single flight:
        - если 100 запросов одновременно прилетели к одному ключу — даём в БД сходить только одному.

5. **Эвикция и управление памятью на разных уровнях**
    - Почему разные уровни могут использовать разные стратегии:
        - LRU/LFU в in‑process кэше;
        - TTL + LRU в Redis;
        - короткий TTL на edge‑уровне.
    - Как читать и использовать статистику по эвикциям и hit‑rate (о чём спрашивают на собесе).

6. **Наблюдаемость и настройка**
    - Какие метрики нужны именно для multilevel:
        - hit‑rate по каждому слою;
        - количество invalidations/second;
        - распределение latency по слоям.
    - Как по метрикам принимать решения: где добавить/убрать уровень, где поменять TTL.

---

## Блок 4 (C). Сложные проблемы кэширования и практические кейсы

Ориентир: 10–15 минут. Здесь логично собрать «тёмную сторону» паттернов из блоков 1–3 и показать, как это обсуждать на собесе.

Базовые проблемы, которые здесь разворачиваем:
- устаревшие данные (stale reads) и рассинхронизация с источником;
- thundering herd / cache stampede;
- тёплый/холодный кэш, прогрев, ресет кластера кэша;
- ограниченный объём и эвикция (LRU/LFU/FIFO/TTL-only и т.д.; связь с паттернами доступа и горячими ключами).

1. **Thundering herd / cache stampede**
    - Сценарий: ключ протух, десятки тысяч запросов одновременно лезут в БД.
    - Типичный anti-pattern: `doom loop` — короткий TTL, отсутствие request coalescing и агрессивные ретраи приводят к тому, что кэш постоянно холодный, запросы лавиной бьют по БД, она тормозит ещё сильнее, и система застревает в порочном круге.
    - Паттерны решения:
        - mutex/lock вокруг заполнения кэша (single flight / request coalescing);
        - «заглядывание вперёд» по TTL (early refresh);
        - jitter/randomized TTL, чтобы ключи не истекали одновременно.
    - Визуализации:
        - график нагрузки на БД и кэш во время stampede;
        - timeline инцидента по минутам: когда истёк ключ, когда вырос QPS, когда включили защиту.
        - отдельная схема `doom loop`: истёк TTL → всплеск miss → рост latency БД → ещё больше истекающих ключей → деградация до полного отказа;
        - такие схемы/графики удобно набрасывать в Excalidraw, оставляя место для пометок по мере рассказа.

2. **Управление устаревшими данными**
    - Где можно жить с сильно устаревшими данными (лента, счётчики) и почему.
    - Где нельзя (деньги, лимиты, баланс, инвентарь) и как тогда строим кэш:
        - короткие TTL + строгие проверки по БД;
        - хранение только вспомогательной информации в кэше;
        - использование версий/ETag'ов.
    - Обратная крайность — `forever caches`: ключи без TTL или с очень большим временем жизни, которые годами никто не инвалидирует; они хорошо снимают нагрузку с БД, но становятся источником тихих, трудно воспроизводимых багов, если через них проходят права, тарифы или промо-правила.

3. **Инциденты из продакшена (примерно 2–3 истории)**
    - Ошибка инвалидации: кэш показывает старую цену/баланс.
    - Неправильный TTL/эвикция → кэш постоянно холодный, система тормозит.
    - Split-brain в кластере кэша → разные ответы для разных пользователей (связать с обсуждением из блока 2).
    - Для каждой истории:
        - как выглядели симптомы;
        - что оказалось root cause;
        - какие изменения в архитектуре/паттернах помогли.

4. **Кэш и собеседования по system design**

   **Мини-сценарий диалога:**

   🎤 **Интервьюер:** "Спроектируйте систему для маркетплейса на 100k RPS.
   Как будете использовать кэш?"

   ❌ **Плохой ответ** (джуниор/мидл без опыта):
   > "Поставлю Redis перед базой. Буду кэшировать всё подряд с TTL 60 секунд.
   >  Если база упадёт, кэш подхватит."

   Проблемы этого ответа:
    - Нет уточняющих вопросов
    - Нет понимания типов данных и их требований
    - "Всё подряд" — плохая стратегия
    - Redis не заменит упавшую БД (eventual consistency ≠ availability)

   ✅ **Хороший ответ** (сеньор/лид):
   > "Сначала уточню несколько вещей:
   >
   > 1. Какие данные самые горячие?
        >    - Карточки товаров? Каталог? Корзины? Сессии?
   > 2. Какие SLA по latency?
        >    - P99 < 100ms? P50 < 50ms?
   > 3. Какая допустимая задержка свежести данных?
        >    - Для денег и остатков — <1 секунды?
   >    - Для каталога — до минуты?
   > 4. Какой бюджет на инфраструктуру?
        >    - Можем ли позволить CDN и многоуровневый кэш?"

   После уточнения предлагаю архитектуру:

   **Многоуровневая схема:**
    - **CDN** (CloudFlare/CloudFront):
        - Статика (JS, CSS, картинки) — TTL 1 день
        - Популярные страницы каталога — TTL 5 минут
        - Hit rate цель: >95%

    - **In-process кэш** (L1, в памяти каждого backend-сервера):
        - Справочники категорий — TTL 1 минута
        - Права и фича-флаги — TTL 30 секунд
        - Размер: 100 MB на сервер, LRU eviction

    - **Redis** (L2, shared cache):
        - Горячие товары (top 10k) — TTL 5 минут
        - Корзины пользователей — TTL 1 час
        - Сессии — TTL по токену
        - Агрегаты (счётчики, топы) — TTL 30 секунд
        - Инвалидация: pub/sub для критичных изменений

   **Для критичных данных** (баланс, остатки на складе):
    - Короткий TTL (5-10 секунд) или read-through с проверкой по БД
    - Версионирование ключей при обновлении
    - Метрики stale reads

   **Защита от проблем:**
    - Thundering herd: single-flight / request coalescing pattern, jittered TTL
    - Hot keys: репликация в Redis, separate tier для топ-100 товаров
    - Cold start: batch pre-warming популярных товаров при деплое

   **Наблюдаемость:**
    - Hit rate по каждому слою (CDN, L1, L2)
    - P99 latency breakdown по слоям
    - QPS на БД при cache miss
    - Alert на thundering herd (QPS spike >2x от baseline)
    - Dashboard с метриками hot keys

   **Тестирование:**
    - Load test с 100k RPS, проверка P99 < 100ms
    - Chaos engineering: симуляция падения Redis, TTL expiry storm
    - Проверка cache warming после deploy

   → **Что видит интервьюер:**
    - Глубину понимания trade-offs
    - Умение задавать правильные вопросы
    - Знание реальных проблем (thundering herd, hot keys)
    - Готовность к продакшену (мониторинг, тесты)
    - Структурное мышление (слои, метрики, защита)

5. **Специфические tricky‑кейсы**
    - Кэширование негативных и «пустых» результатов:
        - 404, пустые поисковые выдачи, «у пользователя нет заказов»;
        - почему это сильно разгружает БД под нагрузкой и какие тут грабли.
    - Кэш и права доступа / feature flags:
        - кэширование ACL, ролей, признаков включённых фич;
        - чем опасны «долгоживущие» ключи, если права уже поменялись.
    - Multi‑tenant‑системы:
        - пример ошибки, когда забыли положить tenant‑id в ключ;
        - к чему это приводит в проде и почему это любимый вопрос на собеседовании.

6. **Кэш в контексте деплоев и миграций**
    - Как вести себя при смене схемы данных или бизнес‑логики:
        - версионирование ключей и постепенное выключение старой схемы;
        - связка с blue‑green деплоем и dual‑write.
    - Какие вопросы задать себе перед релизом, если кэш активно используется.

7. **Чеклист "взрослого" отношения к кэшу**

   Проходите перед запуском кэширования в прод:

   ✅ **1. Определён источник истины и допустимая задержка**
    - Где source of truth? (БД, внешний API, файл?)
    - Максимальное окно рассинхрона? (секунды, минуты?)
    - Пример: "PostgreSQL — source of truth, каталог может отставать до 60 секунд,
      баланс — максимум 1 секунда"

   ✅ **2. Явно описано, ЧТО кэшируем и ЧТО НЕ кэшируем**
    - Список: что идёт в кэш, с какими TTL
    - Анти-список: что сознательно НЕ кэшируем
    - Пример: "Кэшируем: карточки товаров (TTL 5 мин), агрегаты каталога (TTL 1 мин).
      НЕ кэшируем: остатки на складе (read-through с БД), текущий баланс"

   ✅ **3. Стратегия инвалидации задокументирована и протестирована**
    - Как инвалидируем при изменении данных?
    - Что при частичном отказе инвалидации? (fallback на TTL?)
    - Пример: "Pub/sub через Redis на все backend-серверы + fallback TTL 60 секунд.
      Тест: симулируем 500ms delay в pub/sub, проверяем stale rate < 1%"

   ✅ **4. Есть защита от thundering herd**
    - Single-flight / request coalescing в коде
    - Jittered TTL (рандомизация времени истечения)
    - Early refresh (обновление до истечения TTL)
    - Пример: "Golang singleflight.Group для популярных ключей,
      TTL = base ± rand(0, 10%)"

   ✅ **5. Настроен мониторинг и alerts**
   Метрики:
    - Hit rate по слоям (target: >80% для L2, >60% для L1)
    - P99 latency по слоям
    - QPS на БД при cache miss (baseline + alert на >2x spike)
    - Количество stale reads (через версионирование)
    - Memory usage кэша (alert при >80%)

   Alerts:
    - Hit rate упал ниже 60% → проверить TTL, Redis memory
    - QPS на БД вырос >2x → thundering herd?
    - Hot keys detected (>10k RPS на один ключ)

   ✅ **6. Проверен сценарий полного сброса кэша под нагрузкой**
    - Load test с симуляцией: FLUSHALL в Redis
    - Load test с симуляцией: restart всех backend-серверов (L1 сброс)
    - Проверка: БД выдержит ли spike от cold cache?
    - Пример: "Chaos test: FLUSHALL в Redis при 50k RPS.
      Результат: QPS на БД вырос до 30k, P99 latency 200ms (в пределах SLA).
      Cache warming вернул hit rate >80% за 2 минуты"

   ✅ **7. План действий при инциденте с кэшем**
    - Runbook: что делать, если hit rate < 50%?
    - Runbook: что делать при hot key event?
    - Escalation path: когда звать SRE/DBA?
    - Пример: "1. Проверить Redis memory и eviction rate
      2. Проверить TTL в конфиге
      3. Если не помогло — escalate к дежурному SRE
      4. В крайнем случае — отключить проблемный кэш-слой (traffic на БД)"

   Этот чеклист — ваш «паспорт зрелости» кэширования.
   Если на 7 вопросов ответили "да" с примерами — можно в прод.
   Если нет — дорабатывайте архитектуру.

---

Дальше по этому файлу можно:
- уточнять/расширять план (добавлять конкретные примеры и истории);
- набрасывать варианты формулировок для сценария, которые потом переписываются под твой голос.
